#include <stdio.h>
#include <stdlib.h>
//p=prodotto c=contatore


int calcola()
{
    int x,y,c,p;
    for(c=2;c<y;c++)
    {
                    
                    do
                    {
                        x=x+x;
                    }
                    while(c<=y);
                    x=x+x;
    }
    return p;
}

main()
{
      printf("Inserisci un numero ");
      scanf("%d, &x");
      printf("Inserisci un numero");
      scanf("%d, &y");
      int p;
      printf("Il prodotto e' %d", p);
      system("PAUSE");
}
      
      
       
